const cabecera = document.querySelector('h1');
const card = document.querySelector('.card');

//con classList obtenemos todos los elementos de una clase y con add agregamos una clase mas a la lista
card .classList.add('.nueva-clase');

//para borrar una clase.
card.classList.remove('.nueva-clase');

cards = document.querySelectorAll('.card');

console.log(cards)

